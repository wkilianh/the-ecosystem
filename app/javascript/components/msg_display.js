const msgDisplay = () => {
  
 
    // console.log("run")
    // const nickname = document.getElementById('nickname').innerText
    // document.querySelectorAll(".message-container").forEach(elm =>{
    //   if (elm.children[0] !== undefined){
    //     if(elm.children[0].children[0].children[0].innerHTML == nickname){
    //       if (elm.children[0].style.float !== "right"){
    //         console.log(elm.children[0])
    //         elm.children[0].setAttribute("style","float:right;")
    //       }
          
    //     }
    //   } 
    // })
    // document.querySelectorAll(".clickble-name").forEach( (elm) => {
    //   elm.addEventListener("click", (event) => {
    //     msgDisplay()
    //   })
    // })
  }


export {msgDisplay}